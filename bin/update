#!/bin/sh
set -eu

sudo apt -y update
sudo apt -y upgrade
sudo apt autoremove

cd $THETAPI_HOME
git pull
find $THETAPI_HOME/pkg/* -maxdepth 0 -name "[!.]*" -type d -exec bash -c 'tpi $(basename {})' \;

